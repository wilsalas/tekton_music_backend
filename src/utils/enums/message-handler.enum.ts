export enum MessageHandler {
  EMAIL_ALREADY_TAKEN = 'The mail is already in use.',
  USER_NOT_FOUND = 'User not found',
  CREDENTIALS_INCORRECT = 'Your access credentials are incorrect',
  USER_CREATED = 'User created successfully',
}
